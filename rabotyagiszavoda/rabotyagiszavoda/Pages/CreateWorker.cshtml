@page
@model rabotyagiszavoda.Pages.CreateWorker

@{
    ViewData["Title"] = "Create";
}

<h2>Create RABOTYAGA</h2>

<hr/>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Worker.Name" class="control-label"></label>
                <input asp-for="Worker.Name" class="form-control"/>
                <span asp-validation-for="Worker.Name" class="text-danger"></span>
            </div>
            @* <div class="form-group"> *@
@*                 <select> *@
@*                     @foreach (var proj in Model.Project) { *@
@*                          <option>@proj.Title</option> *@
@*                     } *@
@*                     <option></option> *@
@*                 </select> *@
            @* </div> *@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="WorkersPage">Back to List</a>
</div>

@* @section Scripts { *@
    @* @{await Html.RenderPartialAsync("_ValidationScriptsPartial");} *@
@* } *@